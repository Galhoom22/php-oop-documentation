<?php // Opening PHP tag.

class Car {                    // The Car blueprint.
    public string $color;      // The "color" box may only hold a string (text).
    public int $doors;         // The "doors" box may only hold an int (whole number).
}